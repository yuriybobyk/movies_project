import React from 'react';
import {IMovie} from "../interfaces/movie.interface";

interface IProps{
    title: string
    movies: IMovie[]
}

const Row = () => {
    return (
        <div>
            ROW
        </div>
    );
};

export {Row};
