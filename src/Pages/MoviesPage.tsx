import React from 'react';
import {MovieList} from "../components/MovieList";

const MoviesPage = () => {
    return (
        <div>
            <MovieList/>
        </div>
    );
};

export {MoviesPage}
